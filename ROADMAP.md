# Easy Week — Roadmap / хотелки

Живой список идей и задач. Сюда складываем всё, что хочется, чтобы не терять.

## 🔨 В работе
- **Потоковая генерация плана** — отдавать блюда по одному (SSE), а не ждать все сразу.
  Один и тот же вызов DeepSeek с `stream: true`, инкрементальный парс массива `dishes`,
  события `meta → dish → done`. Без роста расхода токенов.
- **Потоковый список покупок** — пункты появляются по мере готовности, тем же приёмом.

## ⏭️ Дальше
- **Привести вёрстку к гайдбуку** — цвета и шрифты уже по гайду, но лейаута нет:
  отступы, сетки, ритм, размеры карточек — везде «на глаз». Свести к единой
  системе из GUIDEBOOK.md (spacing-шкала, контейнеры, вертикальный ритм).
- **Экран «Профиль»** — вкладка есть в навигации, страницы пока нет.
- **Экспорт в JSON для «perdulário»** (подсчёт себестоимости) — ждём формат от Павла.
- **История планов** — отклонённые и черновики старше недели прятать под кнопку «История».
- **Деплой на Pi** — раскатать кулинарный лоадер + метки логов + DeepSeek (git + rsync + restart).
- **HTTPS / домен** — Cloudflare Tunnel для PWA (аккаунт есть, не настроен).

## 💡 Идеи / бэклог
- **Tool calling для модели** — дать модели инструменты, чтобы она сама запрашивала
  данные: получить список уже существующих планов, искать по ним (по блюдам,
  ингредиентам, неделям), тянуть принятые/отклонённые. Тогда диалог сможет
  опираться на реальную историю, а не только на подмешанный в промпт список.
- **Валидатор реалистичности названий** — ловить «Голубая свинина», «Синий суп» и т.п.
- **Валидатор количеств** — ловить абсурд вроде «форель 4 кг на 2 порции».
- **Дашборд расхода токенов** — где смотреть, сколько съедено по моделям.

## ✅ Сделано
- Кулинарный лоадер (кастрюлька с паром) + пул ~100 фраз вместо спиннеров.
- Метки в логах: какой запрос уходит в какой API (провайдер · модель · назначение).
- DeepSeek для плана + короткие шаги; развёрнутые инструкции и список — Cloudflare.
- Список покупок в граммах у источника (без хаоса единиц).
- Локальная разработка в Docker с живыми логами; SW погашен в dev-сборке.
- Экран приветствия, выбор кол-ва блюд, фейд-блюр шапок/футеров, вью плана.
